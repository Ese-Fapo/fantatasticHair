{
  "name": "fantastic-hair",
  "private": true,
  "workspaces": [
    "vite-project"
  ],
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "npm run dev --workspace vite-project",
    "build": "npm run build --workspace vite-project -- --outDir ../dist --emptyOutDir",
    "build:app": "npm run build --workspace vite-project",
    "preview": "npm run preview --workspace vite-project",
    "lint": "npm run lint --workspace vite-project"
  }
}
